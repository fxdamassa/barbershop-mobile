// schedule_page.dart
import 'package:flutter/material.dart';

class SchedulePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agendar Corte'),
      ),
      body: Center(
        child: Text('Página de agendamento de corte'),
      ),
    );
  }
}
